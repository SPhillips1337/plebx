Minimal static frontend for the PlebX MVP.

This simple page fetches `/feed` from the API and displays posts. It's intentionally tiny so it can be served
from a lightweight container during development.

Start locally with:

  docker-compose up --build web
